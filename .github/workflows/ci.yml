name: CI

on:
  pull_request:
  workflow_call:

permissions:
  actions: read
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # check.mjs in published-e2e uses module.registerHooks (stable since 22.13).
      - uses: actions/setup-node@v4
        with:
          node-version: '>=22.13'

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Cache bun dependencies
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('bun.lock') }}
          restore-keys: bun-${{ runner.os }}-

      - run: bun install --frozen-lockfile

      - name: Check code formatting
        run: bunx prettier --check .

      - uses: nrwl/nx-set-shas@v4

      # e2e is published-e2e: the SDK packages installed from a local registry.
      - run: bunx nx affected -t lint test typecheck build api-check e2e
